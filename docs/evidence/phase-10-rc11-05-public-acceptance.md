# RC11 公网部署与最终赛题复验

状态：**PASS**。公网入口为 `https://106.14.13.139`，应用提交与远端 `main` 均为 `02d6f4f85e4657f4fb02f489ff4cc0ae3f01737e`。服务器只加载 `linux/amd64` API/Web 镜像，未上传应用源码；PostgreSQL 与 Caddy 的镜像、启动时间、数据卷和重启次数保持不变，RC10 镜像继续保留为回滚点。

公网真实模型复验覆盖三类自然诉求和“尿酸高＋减重”。减脂、增肌分别展示“轻盈减脂方案”“力量增肌方案”；血糖偏高展示“稳糖调理方案 · 安全替换”；尿酸用例进入 S1/general_only，关闭个体化数值，排除 `high_purine`，并显示医疗免责声明及“建议您在使用本方案前咨询专业医师或注册营养师”。四组回答均由 `openai_compatible / qwen3.7-plus / real` 生成，推荐证据只来自 A/B。

公网 Chromium 中再次完成尿酸安全替换可视验收，健康档案目标仍为未设置，浏览器控制台 0 错误、0 警告。直接 API 测试对话和浏览器测试对话均已删除，数据库中 `RC11-*` 标题计数为 0。镜像传输归档在服务器完成 SHA-256 核验和 `docker load` 后已删除，`incoming` 文件数和服务器应用源码文件数均为 0；本地归档保留用于恢复。

本次全量 Dockerfile 重建被 Docker Hub 元数据 TLS 超时阻断。由于本轮未修改任何依赖文件，发布镜像从已验收的 RC10 `linux/amd64` 镜像增量覆盖当前 API 源码和本地生产 Web standalone 产物；镜像级健康检查和功能 smoke 均通过。未更改 Docker、代理、证书或系统服务的全局配置。

技术交付现已通过。仍须参赛者本人完成三项外部动作：给评委源码访问权或提交平台允许的源码归档、提交比赛表单、记录真实提交时间并从该时刻继续保持服务至少七天。
