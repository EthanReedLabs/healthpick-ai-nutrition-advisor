# RC10 公网部署与总纲验收

状态：**PASS**。公网入口为 `https://106.14.13.139`，源代码与远端 `main` 均为 `b422d7e0ff645d87c7078f9f419062bf443cdfee`。服务器只接收 API/Web 镜像归档，没有上传源码；PostgreSQL 与 Caddy 的镜像、启动时间和重启次数保持不变。

公网实测覆盖三句自然诉求、45 岁减脂方案、空输入、2001 字输入、特殊符号、无关问题、模拟系统异常、A/B/C 来源隔离、真实百炼千问、多轮上下文、账号注册/登录/历史、疾病免责声明和浏览器控制台。平台问题只引用 C；混合问题分别用 C 支撑会员价格、用 A 支撑减脂热量。`我在健身` 的回答由 `qwen3.7-plus / real` 生成并引用 A/B；血糖偏高用例触发 S1 一般信息边界和赛题指定免责声明。

首次公网检查发现“我血糖有点高”没有进入专业提示级安全规则。根因为路由与检索已补同义词，但安全词表未同步，且旧专业提示不是赛题指定原句。修复提交 `b422d7e` 增加三种血糖偏高自然表达、统一强制免责声明并补回归；重新构建和部署后，公网复验显示 `S1/general_only`、规则 `message_blood_glucose_concern`、A/B 引用及完整指定文案，问题闭环。

所有直接 API 测试对话、浏览器测试对话和临时测试账号均已删除；镜像传输归档已从服务器删除，本地仍保留可恢复副本。干净浏览器会话控制台为 0 错误、0 警告。详细机器可读证据见同名 JSON。

可用期按 `max(实际比赛提交时间 + 7 天, 2026-08-30 01:00 +08:00)` 执行。实际提交表单时间不得伪造，参赛者提交后应把真实时间补入可用性记录并据此确定最终停机时间。
