# P08-06 短期公网可用性责任确认

## 结论

P08-06 按参赛者确认的短期比赛演示边界以 `PASS` 完成。环境继续运行一段时间，比赛或演示使用结束后由参赛者手动停机；不安装 systemd 定时器、不增加外部监控账号，也不引入持续模型调用或额外运维复杂度。

## 当前保障

- `https://106.14.13.139` 首页与 `/healthz` 在 `2026-08-22T17:11:59+08:00` 均返回 200。
- 健康响应为 `status=ok`、`llm=real`，数据库 schema 和模型配置就绪。
- PostgreSQL、API、Web 均有容器健康检查；PostgreSQL、API、Web、Caddy 使用 `restart: unless-stopped`。
- Caddy 的数据卷持久化，继续负责短期公网 IP 证书自动续期。
- 未安装任何全局 systemd unit，服务器全局配置保持不变。

## 运行与停机责任

参赛者负责在比赛/演示使用期保持 ECS 开机和安全组 80/443 可达，结束后手动停机。当前确认只授权未来停机，不授权删除数据库卷、服务器文件或发布证据；如需删除数据，必须再次明确确认。

机器可读证据见 `docs/evidence/phase-08-p08-06-availability.json`。
