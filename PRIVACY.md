# HealthPick 隐私说明

生效日期：2026-08-22。适用范围：本竞赛版本的 HealthPick Web 与 API。

## 1. 我们处理什么数据

匿名使用时，系统生成随机会话 ID，并在 PostgreSQL 保存会话标题、用户消息、AI 回答、引用、路由、模型标识和必要的时间/请求 ID。注册时另保存规范化邮箱、加盐密码哈希、认证令牌哈希和到期时间；服务端不保存明文密码或明文 bearer token。

健康目标、年龄段、可选精确年龄、身高、体重、活动水平、饮食偏好、过敏和特殊情况用于当前回答和安全判断。当前版本的健康档案为 `ephemeral`，不单独写入 PostgreSQL；但用户在聊天正文中主动输入的健康信息会作为对话内容持久化。

浏览器 `localStorage` 保存匿名会话 ID；账号登录后还保存邮箱和 30 天有效的 bearer token，以便刷新后恢复历史。不要在共享或不受信任设备上保持登录，使用完毕应退出账号并清除浏览器站点数据。

## 2. 数据怎样流动

浏览器通过 API 发送输入。API 先执行校验、来源路由和医疗安全门，再把用户消息、经筛选的 A/B/C 证据以及必要的临时档案约束发送给配置的模型 Provider。当前公网部署使用阿里云百炼北京地域 Token Plan 的 `qwen3.7-plus`，相应请求内容会发送到该远程 Provider 并受其条款约束；默认本地 Compose 可使用宿主 Ollama 复现。HealthPick 不接入广告、支付、相机、麦克风、设备健康平台或第三方统计 SDK。

PostgreSQL 保存账号和对话数据。应用日志记录 request ID、状态、路由、风险规则标签和耗时等诊断信息，测试门禁止记录原始健康文本、完整档案、令牌、密码和模型密钥。

完整机器可读路径见 `docs/release-data-map.json`。

## 3. 用途与共享

数据仅用于提供聊天、引用、历史恢复、账号迁移、导出/删除、安全防护和故障诊断。项目不出售个人数据，不用于广告画像。除阿里云 ECS、同机 PostgreSQL 和已明确披露的百炼模型 Provider 外，不主动向第三方共享；比赛提交页面或仓库维护渠道作为当前责任联系入口。

## 4. 保存期限

认证令牌服务端有效期为 30 天，退出会撤销当前令牌。账号和对话当前不会按 `.env` 自动 30 天清理，而是保存到用户主动删除、账号永久删除或竞赛运营方清理演示环境为止。比赛演示结束后，运营方应删除测试账号和非必要对话。此限制被明确披露，未把尚未实现的自动保留任务写成现有能力。

## 5. 导出、删除和更正

- 登录后打开“账号”，可下载账号、认证会话、匿名/账号会话、对话与轮次的 JSON 导出。
- 可在历史列表删除单条会话；删除会级联删除其中全部轮次。
- “永久删除账号”要求再次输入密码，随后删除用户、认证会话、所绑定匿名会话、对话和轮次；静态知识库不会被删除。
- 匿名用户可逐条删除本机匿名会话下的对话。仅清除浏览器 `localStorage` 会失去会话 ID，但不会自动删除服务端内容，应先在页面删除历史。
- 健康档案是请求内临时状态，刷新即不再作为独立档案保存；聊天正文中的同类信息仍按对话删除规则处理。

## 6. 安全措施与限制

服务端使用 scrypt 加盐密码哈希和 SHA-256 令牌哈希，资源访问进行账号/会话归属校验，生产配置要求 HTTPS、PostgreSQL 和真实模型。仓库扫描真实 Secret，错误响应不返回堆栈、SQL、Prompt 或密钥。

当前 Web 使用 `localStorage` bearer token，而非 HttpOnly Cookie；因此浏览器或同源脚本被攻破时存在令牌暴露风险。项目不渲染原始 HTML、不加载业务第三方脚本，并建议只在可信设备使用竞赛账号。正式长期运营前应迁移到 HttpOnly/Secure/SameSite Cookie 与相应 CSRF 防护。

## 7. 未成年人和医疗边界

本项目不面向未成年人单独提供个性化处方。未成年人、确诊疾病、孕哺期等进入受限模式。HealthPick 不是医疗器械，不诊断、不调整药物、不承诺治疗效果，也不能替代医生或注册营养师。

## 8. 联系与更新

隐私请求或漏洞请通过比赛提交页面列明的项目负责人或代码仓库维护渠道联系，不要在公开 Issue 中粘贴密码、令牌、健康对话或其他敏感内容。隐私说明随源码公开且无需登录即可阅读；长期运营前仍应补充独立的公开隐私联系地址。

若引入远程 Provider、分析/崩溃 SDK、云同步、新数据类别或新的共享方，必须先更新机器数据路径、本文和部署说明，再重新执行 P08 文档与安全门。
